# Option A) Parser en LOCAL des pages HTML enregistrées manuellement :
#   - enregistre les pages résultats/annonces SeLoger via ton navigateur dans data/html/
#   - lance:  python src/parse_local_html.py
#
# Option B) Récupérer une petite liste d'URL d'annonces (≤20) via HTTP à faible cadence :
#   - mets 20 URLs max dans data/urls.txt (une par ligne)
#   - lance:  python src/fetch_ads.py  (respecte les CGU, pas de contournement)
#
# Limites (sécurité/éthique) :
# - Volume très faible (≤20)
# - Pas d'anti-bot, pas de proxy, pas de headless-stealth, pas de CAPTCHA solving.
# - Usage éducatif/personnel.
REQUEST_DELAY=2.5  # secondes entre requêtes (Option B)
